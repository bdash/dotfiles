for file in ~/.bash/{path,env,history,prompt,z/z.sh}; do
    [[ -r "$file" ]] && . "$file"
done

for file in ~/.bash/completion/*; do
    [[ -r "$file" ]] && . "$file"
done

# Disable flow control.
stty -ixon
